<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:conv="clr-namespace:WAVE.App.Converters">

    <!-- Conversores -->
    <BooleanToVisibilityConverter x:Key="BoolToVis" />
    <conv:StateToBrushConverter x:Key="StateToBrush" />

    <!-- Paleta / feedback de estado (spec: cinza, amarelo, verde, vermelho) -->
    <SolidColorBrush x:Key="BrushIdle" Color="#FFB0BEC5" />
    <SolidColorBrush x:Key="BrushConnecting" Color="#FFFFC107" />
    <SolidColorBrush x:Key="BrushRunning" Color="#FF43A047" />
    <SolidColorBrush x:Key="BrushFailed" Color="#FFE53935" />

    <SolidColorBrush x:Key="BrushBackground" Color="#FF0A0F14" />
    <SolidColorBrush x:Key="BrushSurface" Color="#FF13202B" />
    <SolidColorBrush x:Key="BrushText" Color="#FFECEFF1" />
    <SolidColorBrush x:Key="BrushMuted" Color="#FF8899A6" />
    <SolidColorBrush x:Key="BrushAccent" Color="#FF4CC2FF" />

    <!-- Botão de rede: alvo de toque >= 60x60, cantos arredondados, cor por estado -->
    <Style x:Key="TouchButtonStyle" TargetType="Button">
        <Setter Property="MinWidth" Value="60" />
        <Setter Property="MinHeight" Value="60" />
        <Setter Property="Foreground" Value="#FF102027" />
        <Setter Property="FontSize" Value="14" />
        <Setter Property="Cursor" Value="Hand" />
        <Setter Property="Padding" Value="12" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border x:Name="Root"
                            CornerRadius="10"
                            Background="{TemplateBinding Background}"
                            Padding="{TemplateBinding Padding}"
                            SnapsToDevicePixels="True">
                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter TargetName="Root" Property="Opacity" Value="0.45" />
                        </Trigger>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="Root" Property="Opacity" Value="0.9" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- Botão de ação da barra superior -->
    <Style x:Key="ActionButtonStyle" TargetType="Button">
        <Setter Property="MinHeight" Value="40" />
        <Setter Property="Padding" Value="16,8" />
        <Setter Property="Foreground" Value="{StaticResource BrushText}" />
        <Setter Property="Background" Value="{StaticResource BrushSurface}" />
        <Setter Property="Cursor" Value="Hand" />
        <Setter Property="FontSize" Value="14" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border x:Name="Root" CornerRadius="8" Background="{TemplateBinding Background}"
                            Padding="{TemplateBinding Padding}" BorderBrush="{StaticResource BrushAccent}" BorderThickness="1">
                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter TargetName="Root" Property="Opacity" Value="0.4" />
                        </Trigger>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="Root" Property="Opacity" Value="0.85" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="HeadingText" TargetType="TextBlock">
        <Setter Property="Foreground" Value="{StaticResource BrushText}" />
        <Setter Property="FontSize" Value="16" />
        <Setter Property="FontWeight" Value="SemiBold" />
        <Setter Property="Margin" Value="0,0,0,8" />
    </Style>

    <Style x:Key="MutedText" TargetType="TextBlock">
        <Setter Property="Foreground" Value="{StaticResource BrushMuted}" />
        <Setter Property="FontSize" Value="12" />
    </Style>

</ResourceDictionary>
