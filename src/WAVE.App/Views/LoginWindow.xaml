<Window x:Class="WAVE.App.Views.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="WAVE" Height="460" Width="400"
        WindowStartupLocation="CenterScreen" ResizeMode="NoResize"
        Background="{StaticResource BrushBackground}" Foreground="{StaticResource BrushText}"
        FontFamily="Segoe UI">
    <Border Padding="26">
        <StackPanel>
            <TextBlock Text="WAVE" FontSize="26" FontWeight="Bold" Foreground="{StaticResource BrushAccent}" />
            <TextBlock Text="{Binding Title}" Style="{StaticResource HeadingText}" Margin="0,4,0,18" TextWrapping="Wrap" />

            <TextBlock Text="Login" Style="{StaticResource MutedText}" />
            <TextBox x:Name="UsernameInput" Height="30" Margin="0,2,0,10"
                     Text="{Binding Username, UpdateSourceTrigger=PropertyChanged}" />

            <StackPanel Visibility="{Binding ShowSetupFields, Converter={StaticResource BoolToVis}}">
                <TextBlock Text="Nome de exibição" Style="{StaticResource MutedText}" />
                <TextBox x:Name="DisplayNameInput" Height="30" Margin="0,2,0,10"
                         Text="{Binding DisplayName, UpdateSourceTrigger=PropertyChanged}" />
            </StackPanel>

            <TextBlock Text="Senha" Style="{StaticResource MutedText}" />
            <PasswordBox x:Name="PasswordInput" Height="30" Margin="0,2,0,10" />

            <StackPanel Visibility="{Binding ShowSetupFields, Converter={StaticResource BoolToVis}}">
                <TextBlock Text="Confirmar senha" Style="{StaticResource MutedText}" />
                <PasswordBox x:Name="ConfirmInput" Height="30" Margin="0,2,0,10" />
            </StackPanel>

            <TextBlock Text="{Binding Error}" Foreground="{StaticResource BrushFailed}" TextWrapping="Wrap"
                       Margin="0,2,0,10" Visibility="{Binding HasError, Converter={StaticResource BoolToVis}}" />

            <Button Content="{Binding ActionText}" Click="OnSubmit" IsDefault="True"
                    Style="{StaticResource ActionButtonStyle}" Height="42" Margin="0,6,0,0" />
        </StackPanel>
    </Border>
</Window>
